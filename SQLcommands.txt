CREATE TABLE users(fbid bigint PRIMARY KEY, LastName varchar(100),
FirstName varchar(100),
College varchar(100),
cityIntern varchar(100),
specificPartCity varChar(100),
genderPref varchar(20),
foundHousing bool,
housingPref varchar(100),
company varchar(100),
internJob varchar(100),
moreInfo TEXT,
checkItemCook bool,
checkItemParty bool,
checkItemSmoke bool,
checkItemQuiet bool,
checkItemNightOwl bool,
checkItemMorningBird bool)